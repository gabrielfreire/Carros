<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cor primária do aplicativo. -->
        <item name="colorPrimary">@color/primary</item>

        <!-- Variação escura da cor primária para status bar e contextual app bars. -->
        <item name="colorPrimaryDark">@color/primary_dark</item>

        <!-- Cor de acentuação para as viwes. (checkbox, textfield etc) -->
        <item name="colorAccent">@color/accent</item>

        <!-- Cor para o efeito ripple -->
        <item name="colorControlHighlight">@color/control_highlight</item>
    </style>

</resources>
